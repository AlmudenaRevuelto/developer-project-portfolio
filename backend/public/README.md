# Public

Public entry point of the backend application.

## Responsibility
This directory contains the front controller (`index.php`) that:
- Receives HTTP requests
- Routes them to the appropriate controller
- Acts as the single entry point of the backend
