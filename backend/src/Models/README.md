# Models

Domain models representing the core entities of the application.

## Current Models
- `Client` – Represents a client entity with basic attributes

## Notes
Models are simple PHP objects and do not contain database logic.
