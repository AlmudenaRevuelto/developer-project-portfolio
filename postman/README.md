## API Testing with Postman

1. Import the collection from:
   postman/client-project-manager.postman_collection.json

2. Import the environment from:
   postman/local-docker.postman_environment.json

3. Select the "Local Docker" environment.

4. Ensure Docker is running:
   docker compose up

Base URL:
http://localhost:8000